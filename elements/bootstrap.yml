id: bootstrap
version: 2307
about: rlxos bootstrap toolchain

build-time:
  - core/kernel-headers.yml
  - bootstrap/glibc-stage1.yml
  - bootstrap/binutils-stage1.yml
  - bootstrap/gcc-stage1.yml
  - bootstrap/glibc-stage2.yml
  - bootstrap/binutils-stage2.yml
  - bootstrap/gcc-stage2.yml

script: |
  mkdir -p %{install-root}
  echo %{version} > %{install-root}/.bootstrap
