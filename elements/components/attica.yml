id: attica
version: 5.94.0
about: KF5 attica

configure: >
  -DCMAKE_BUILD_TYPE=Release
  -DBUILD_TESTING=OFF
  -Wno-dev

depends:
  - components/qt.yml
build-time:
  - components/extra-cmake-modules.yml
sources:
  - https://download.kde.org/stable/frameworks/%{version}/%{id}-%{version}.tar.xz

# TODO: check update url