id: bolt
version: 0.0.2
about: Bolt Virtual Assistant

build-type: go-pkg
variables:
  go-pkg: cmd/bolt

post-script: |
  install -vDm0644 /files/bolt/responses.txt %{install-root}/%{datadir}/bolt/responses
