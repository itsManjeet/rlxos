id: bolt
version: 0.0.1
about: Bolt Virtual Assistant

build-type: go-pkg
variables:
  go-pkg: cmd/bolt

post-script: |
  install -vDm0644 -t %{install-root}/%{localstatedir}/lib/bolt/ /files/bolt/responses.txt