id: make
version: 4.4
about: |
  A program for controlling the generation of executables and other non-source files of a package from source files

depends:
  - components/core/glibc.yml

sources:
  - https://ftp.gnu.org/gnu/make/make-%{version}.tar.gz
