id: mtools
version: 4.0.43
about: Utilities to access MS-DOS disks without mounting them
sources:
  - https://ftp.gnu.org/gnu/mtools/mtools-%{version}.tar.bz2

depends:
  - components/core/glibc.yml
  - components/core/bash.yml
