id: dconf-editor
version: 45.0.1
about: A low-level configuration system editor

depends:
  - components/gtk.yml
  - components/dconf.yml
build-depends:
  - components/vala.yml
sources:
  - https://download.gnome.org/sources/dconf-editor/%{version:2}/dconf-editor-%{version}.tar.xz

# TODO: check version