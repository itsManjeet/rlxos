id: dconf-editor
version: 43.0
about: A low-level configuration system editor

depends:
  - components/gtk.yml
  - components/dconf.yml
build-time:
  - components/vala.yml
sources:
  - https://download.gnome.org/sources/dconf-editor/%{version:1}/dconf-editor-%{version}.tar.xz

# TODO: check version