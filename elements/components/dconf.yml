id: dconf
version: 0.40.0
about: |
  A low-level configuration system

depends:
  runtime:
    - dbus
    - glib
    - libxml2
    - libxslt

  buildtime:
    - vala

sources:
  - https://download.gnome.org/sources/dconf/0.40/dconf-0.40.0.tar.xz

packages:
  - id: pkg
    dir: dconf-0.40.0

    flags:
      - id: configure
        value: -D bash_completion=false