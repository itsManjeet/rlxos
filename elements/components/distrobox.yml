id: distrobox
version: 1.7.2.1
about: Use any Linux distribution inside your terminal

script: |-
  ./install -P %{install-root}/%{prefix}

depends:
  - components/docker.yml

sources:
  - distrobox-%{version}.tar.gz::https://github.com/89luca89/distrobox/archive/%{version}.tar.gz
