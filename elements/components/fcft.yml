id: fcft
version: 3.1.8
about: Simple library for font loading and glyph rasterization

sources:
  - fcft-%{version}.tar.gz::https://codeberg.org/dnkl/%{id}/archive/%{version}.tar.gz

depends:
  - components/fontconfig.yml
  - components/harfbuzz.yml
  - components/pixman.yml
build-depends:
  - components/tllist.yml
  - components/meson.yml
