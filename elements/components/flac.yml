id: flac
version: 1.4.3
about: An audio CODEC similar to MP3, but lossless

configure: >-
  --disable-thorough-tests

depends:
  - components/glibc.yml

sources:
  - https://downloads.xiph.org/releases/flac/flac-%{version}.tar.xz
  - https://www.linuxfromscratch.org/patches/blfs/svn/flac-%{version}-security_fixes-1.patch
