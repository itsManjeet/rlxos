id: gnome-power-manager
version: 3.32.0
about:
  Power Manager package contains a tool used to report on power management on
  the system

depends:
  - components/gtk.yml
  - components/upower.yml
sources:
  - https://download.gnome.org/sources/gnome-power-manager/%{version:2}/gnome-power-manager-%{version}.tar.xz

configure: >-
  --buildtype=release
