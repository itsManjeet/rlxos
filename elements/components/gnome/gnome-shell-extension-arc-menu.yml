id: gnome-shell-extension-arc-menu
version: v20
about: Application Menu extension for GNOME
release: 0
depends:
  - components/gnome/gnome-menus.yml
  - components/glib.yml
sources:
  - https://gitlab.com/arcmenu/ArcMenu/-/archive/%{version}/ArcMenu-%{version}.tar.gz
script: make install DESTDIR=%{install-root}
