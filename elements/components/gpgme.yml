id: gpgme
version: 1.22.0
about: A C library that allows cryptography support to be added to a program

configure: >-
  --disable-gpg-test

depends:
  - components/libassuan.yml
sources:
  - https://www.gnupg.org/ftp/gcrypt/gpgme/gpgme-%{version}.tar.bz2
