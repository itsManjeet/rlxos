id: jansson
version: 2.14
about: Package contains a library used to encode, decode, and manipulate JSON data

configure: >-
  --disable-static

sources:
  - https://digip.org/jansson/releases/jansson-%{version}.tar.gz
