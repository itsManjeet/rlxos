id: json-glib
version: 1.6.6
about:
  A library providing serialization and deserialization support for the JavaScript
  Object Notation (JSON) format described by RFC 4627

depends:
  - components/glib.yml
build-time:
  - components/gobject-introspection.yml
sources:
  - https://download.gnome.org/sources/json-glib/%{version:1}/json-glib-%{version}.tar.xz
