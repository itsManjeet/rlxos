id: json-glib
version: 1.8.0
about: serialization and deserialization support for the JavaScript

depends:
  - components/glib.yml
build-depends:
  - components/gobject-introspection.yml
sources:
  - https://download.gnome.org/sources/json-glib/%{version:2}/json-glib-%{version}.tar.xz
