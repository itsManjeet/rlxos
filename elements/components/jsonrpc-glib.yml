id: jsonrpc-glib
version: 3.44.0
about: A JSON rpc library for glib

depends:
  - components/json-glib.yml
build-depends:
  - components/gobject-introspection.yml
  - components/meson.yml
  - components/vala.yml
sources:
  - https://gitlab.gnome.org/GNOME/jsonrpc-glib/-/archive/%{version}/jsonrpc-glib-%{version}.tar.gz
