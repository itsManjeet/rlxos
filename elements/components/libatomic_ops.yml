id: libatomic_ops
version: 7.8.2
about: Implementations for atomic memory update operations on a number of architectures

configure: >-
  --disable-static
  --enable-shared

depends:
  - components/glibc.yml

sources:
  - https://github.com/ivmai/libatomic_ops/releases/download/v%{version}/libatomic_ops-%{version}.tar.gz
