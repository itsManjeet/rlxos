id: libcacard
version: 2.8.1
about:
  (Common Access Card) library thar provides emulation of smart cards to a virtual
  card reader running in a guest virtual machine

depends:
  - components/glib.yml
  - components/nss.yml
sources:
  - https://www.spice-space.org/download/libcacard/libcacard-%{version}.tar.xz
