id: libcloudproviders
version: 0.3.6
about: Library which provides a DBus API that allows cloud storage sync clients to expose their services

build-depends:
  - components/gobject-introspection.yml
  - components/vala.yml
  - components/py/py-packaging.yml

depends:
  - components/glib.yml

sources:
  - https://download.gnome.org/sources/libcloudproviders/%{version:2}/libcloudproviders-%{version}.tar.xz
