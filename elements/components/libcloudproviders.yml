id: libcloudproviders
version: 0.3.2
about: Library which provides a DBus API that allows cloud storage sync clients to expose their services

build-depends:
  - components/gobject-introspection.yml
  - components/vala.yml

depends:
  - components/glib.yml

sources:
  - https://download.gnome.org/sources/libcloudproviders/%{version:1}/libcloudproviders-%{version}.tar.xz
