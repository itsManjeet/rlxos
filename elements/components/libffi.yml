id: libffi
version: 3.4.6
about: Library provides a portable, high level programming interface to various calling conventions

configure: >-
  --disable-static
  --with-gcc-arch=native

depends:
  - components/glibc.yml

sources:
  - https://github.com/libffi/libffi/releases/download/v%{version}/libffi-%{version}.tar.gz
