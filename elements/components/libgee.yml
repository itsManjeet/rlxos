id: libgee
version: 0.20.6
about:
  A collection library providing GObject based interfaces and classes for commonly
  used data structures

depends:
  - components/glib.yml
build-time:
  - components/gobject-introspection.yml
  - components/vala.yml
sources:
  - https://download.gnome.org/sources/libgee/%{version:1}/libgee-%{version}.tar.xz
