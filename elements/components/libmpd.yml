id: libmpd
version: 11.8.17
about: Signal based wrapper around libmpdclient

depends:
  - components/glib.yml

sources:
  - https://download.sarine.nl/Programs/gmpc/%{version:1}/libmpd-%{version}.tar.gz

# TODO: check issues and add patches