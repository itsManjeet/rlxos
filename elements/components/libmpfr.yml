id: libmpfr
version: 4.2.0
about: Library for multiple precision math

depends:
  runtime:
  - libgmp

sources:
- https://ftp.gnu.org/gnu/mpfr/mpfr-{{version}}.tar.xz

build-dir: mpfr-{{version}}
configure: >
  --enable-thread-safe
  --disable-static