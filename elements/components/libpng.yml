id: libpng
version: 1.6.43
about: Libraries used by other programs for reading and writing PNG files
sources:
  - https://downloads.sourceforge.net/libpng/libpng-%{version}.tar.xz

depends:
  - components/glibc.yml

build-type: autotools

variables:
  run-autogen: false
