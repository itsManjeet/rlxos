id: libsass
version: 3.6.5
about: A CSS pre-processor language

pre-script: |-
  autoreconf -fi

depends:
  - components/glibc.yml

sources:
  - https://github.com/sass/libsass/archive/%{version}/libsass-%{version}.tar.gz
