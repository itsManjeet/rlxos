id: libsass
version: 3.6.5
about: |
  A CSS pre-processor language
release: 0
sources:
- https://github.com/sass/libsass/archive/%{version}/libsass-%{version}.tar.gz
script: |
  autoreconf -fi
  ./configure --prefix=/usr
  make
  make DESTDIR=%{install-root} install

