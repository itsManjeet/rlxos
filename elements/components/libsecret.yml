id: libsecret
version: 0.20.4
about: |
  A GObject based library for accessing the Secret Service API

build-type: meson

configure: >
  --buildtype=release

depends:
  - components/glib.yml
build-time:
  - components/gobject-introspection.yml
  - components/libgcrypt.yml
  - components/vala.yml
  - components/gtk-doc.yml

sources:
  - https://download.gnome.org/sources/libsecret/0.20/libsecret-%{version}.tar.xz

