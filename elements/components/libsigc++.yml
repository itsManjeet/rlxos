id: libsigc++
version: 2.10.7
about: A typesafe callback system for standard C++

variables:
  run-autogen: false

sources:
  - https://download.gnome.org/sources/libsigc++/%{version:1}/libsigc++-%{version}.tar.xz

depends:
  - components/glibc.yml
