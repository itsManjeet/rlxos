id: libsigc++2.12
version: 2.12.1
about: A typesafe callback system for standard C++

variables:
  run-autogen: false

sources:
  - https://download.gnome.org/sources/libsigc++/%{version:2}/libsigc++-%{version}.tar.xz

depends:
  - components/glibc.yml

