id: libspectre
version: 0.2.12
about: Small library for rendering Postscript documents

seperate-build-dir: false
configure: >-
  --disable-static

depends:
  - components/ghostscript.yml

sources:
  - https://libspectre.freedesktop.org/releases/libspectre-%{version}.tar.gz
