id: libunwind
version: 1.6.2
about: Determine and manipulate the call-chain of a program

depends:
  - components/glibc.yml
  - components/xz.yml
  - components/zlib.yml

sources:
  - https://github.com/libunwind/libunwind/releases/download/v%{version}/libunwind-%{version}.tar.gz
