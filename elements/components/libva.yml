id: libva
version: 2.22.0
about: Library provides access to hardware accelerated video processing

depends:
  - components/libdrm.yml
  - components/mesa.yml

sources:
  - https://github.com/intel/libva/releases/download/%{version}/libva-%{version}.tar.bz2
