id: libyaml-cpp
version: 0.6.3
about: YAML parser and emitter in c++
release: 0
depends:
- components/glibc.yml
build-time:
- components/cmake.yml
- components/gcc.yml
sources:
- https://github.com/jbeder/yaml-cpp/archive/yaml-cpp-%{version}.tar.gz
configure: |
  -DBUILD_SHARED_LIBS=ON -DYAML_BUILD_SHARED_LIBS=ON -DCMAKE_BUILD_TYPE=Release -DYAML_CPP_BUILD_TESTS=OFF -DYAML_CPP_BUILD_TOOLS=OFF

