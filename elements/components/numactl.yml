id: numactl
version: 2.0.14
about: Simple NUMA policy support
release: 0
depends:
- components/glibc.yml
sources:
- https://github.com/numactl/numactl/releases/download/v%{version}/%{id}-%{version}.tar.gz

