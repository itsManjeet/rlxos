id: pangomm
version: 2.54.0
about: A C++ interface to Pango

variables:
  run-autogen: false

depends:
  - components/cairomm.yml
  - components/glibmm.yml
  - components/pango.yml
sources:
  - https://download.gnome.org/sources/pangomm/%{version:2}/pangomm-%{version}.tar.xz
