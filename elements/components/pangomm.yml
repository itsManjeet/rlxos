id: pangomm
version: 2.46.3
about: A C++ interface to Pango

variables:
  run-autogen: false

depends:
  - components/cairomm.yml
  - components/glibmm.yml
  - components/pango.yml
sources:
  - https://download.gnome.org/sources/pangomm/%{version:1}/pangomm-%{version}.tar.xz
