id: pixman
version: 0.42.2
about: Library that provides low-level pixel manipulation features such as image compositing and trapezoid rasterization
sources:
  - https://www.cairographics.org/releases/pixman-%{version}.tar.gz

depends:
  - components/glibc.yml

build-depends:
  - components/libpng.yml
  - components/meson.yml
