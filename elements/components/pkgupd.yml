id: pkgupd
merge: [version.yml]
about: Package Management and Updater Daemon
commit: 4b9e6d773deca1d95e8e2bee39f976a20e08d967

pre-script: |-
  sed -i 's#${YAML_CPP_LIBRARIES}#yaml-cpp#g' src/CMakeLists.txt
post-script: |-
  install -v -D -m 0644 /dev/stdin %{install-root}/%{sysconfdir}/%{id}.yml << "EOF"
  version: %{version}
  EOF

depends:
  - components/glibc.yml
  - components/libyaml-cpp.yml
  - components/curl.yml
  - components/ostree.yml

build-depends:
  - components/git.yml  

sources:
  - pkgupd-%{commit}.tar.gz::https://github.com/itsManjeet/pkgupd/archive/%{commit}.tar.gz
