id: pkgupd
version: 0.1.0
about: Package Management and Updater Daemon

commit: 92c89f465617387e03942976aa63742b53f8dc54

post-script: |-
  install -v -D -m 0755 -t %{install-root}%{bindir} target/release/%{id}

depends:
  - components/glibc.yml

build-depends:
  - components/git.yml

sources:
  - pkgupd-%{commit}.tar.gz::https://github.com/itsManjeet/pkgupd/archive/%{commit}.tar.gz
