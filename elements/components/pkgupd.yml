id: pkgupd
version: 0.1.0
about: Package Management and Updater Daemon
commit: 608c22190a55de495d2243be29ff323ed9945a55

post-script: |-
  rm -rf %{install-root}/%{libdir} %{install-root}/%{prefix}/include

depends:
  - components/glibc.yml

build-depends:
  - components/git.yml
  - components/libyaml-cpp.yml

sources:
  - pkgupd-%{commit}.tar.gz::https://github.com/itsManjeet/pkgupd/archive/%{commit}.tar.gz
