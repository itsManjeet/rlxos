id: sshfs
version: 3.7.3
about: FUSE client based on the SSH File Transfer Protocol

depends:
  - components/fuse.yml
  - components/glib.yml
  - components/openssh.yml
build-depends:
  - components/meson.yml
sources:
  - https://github.com/libfuse/sshfs/releases/download/%{id}-%{version}/sshfs-%{version}.tar.xz
