id: swupd
version: 0.1.0
about: Software Updater Daemon

variables:
  commit: 52a4c346e47803137ea9e761538bf9e21c60f615

post-script: |-
  install -v -D -m 0755 -t %{install-root}%{bindir} target/release/%{id}

depends:
  - components/ostree.yml

build-depends:
  - components/rustc.yml

sources:
  - swupd-%{commit}.tar.gz::https://github.com/itsManjeet/swupd/archive/%{commit}.tar.gz
