id: typescript
version: 4.7.4
about: TypeScript is a language for application scale JavaScript development
release: 0
depends:
- components/nodejs.yml
sources:
- typescript-%{version}.noextract::https://registry.npmjs.org/%{id}/-/%{id}-%{version}.tgz
script: "mv ${PKGUPD_SRCDIR}/typescript-%{version}.noextract typescript-%{version}.tgz\n\nnpm
  install -g --prefix %{install-root}/usr typescript-%{version}.tgz\nchmod -R u=rwX,go=rX
  \"%{install-root}\"\nchown -R root:root %{install-root} "

