id: wayland-protocols
version: 1.36
about: Project to define a protocol for a compositor to talk to its clients as well as a library implementation of the protocol

build-depends:
  - components/meson.yml
  - components/wayland.yml

sources:
  - https://gitlab.freedesktop.org/wayland/wayland-protocols/-/releases/%{version}/downloads/wayland-protocols-%{version}.tar.xz
