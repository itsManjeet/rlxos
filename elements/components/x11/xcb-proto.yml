id: xcb-proto
version: 1.15.2
about: XML-XCB protocol descriptions that libxcb uses to generate the majority of its code and API

pre-script: rm autogen.sh

sources:
  - https://xorg.freedesktop.org/archive/individual/proto/xcb-proto-%{version}.tar.xz

build-time:
  - core/glibc.yml
  - core/libxml2.yml
  - core/python.yml