id: zlib
version: 1.2.13
about: Compression and decompression routines

post-script: |
  rm -fv ${pkgupd_pkgdir}/usr/lib/libz.a

depends:
  - core/glibc.yml

sources:
  - zlib-%{version}.tar.gz::https://github.com/madler/zlib/archive/refs/tags/v%{version}.tar.gz
