id: libportal
version: 0.4
about: A library that contains GIO-style async APIs for most Flatpak portals
sources:
- https://github.com/flatpak/libportal/releases/download/0.4/libportal-0.4.tar.xz
build-dir: libportal-0.4
depends:
  runtime:
  - glib
configure: --buildtype=release -D gtk_doc=false
