id: pciutils
version: 3.7.0
about: Tools for listing PCI devices, inspecting their status and setting their configuration
  registers
sources:
- https://www.kernel.org/pub/software/utils/pciutils/pciutils-3.7.0.tar.xz
build-dir: pciutils-3.7.0
depends:
  runtime:
  - glibc
  - kmod
script: 'make PREFIX=/usr SHARED=yes SBINDIR=/usr/bin SHAREDIR=/usr/share/hwdata MANDIR=/usr/share/man

  make PREFIX=/usr SHARED=yes SBINDIR=/usr/bin SHAREDIR=/usr/share/hwdata MANDIR=/usr/share/man
  install install-lib DESTDIR=${pkgupd_pkgdir}

  chmod -v 755 ${pkgupd_pkgdir}/usr/lib/libpci.so


  install -v -D -m 0644 ${FILES}/pciutils/update-pciids.* -t ${pkgupd_pkgdir}/usr/lib/systemd/system/'
