id: poppler-data
version: 21.07.0
about: |
  Poppler data

depends:
  runtime:
    - poppler

sources:
  - https://poppler.freedesktop.org/poppler-data-0.4.10.tar.gz

build-dir: poppler-data-0.4.10
script: |
  make prefix=/usr DESTDIR=${pkgupd_pkgdir} install