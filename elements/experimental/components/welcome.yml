id: welcome
version: main
about: rlxos welcome app
sources:
- welcome-{{version}}.tar.gz::https://github.com/itsManjeet/welcome/archive/refs/heads/{{version}}.tar.gz
build-dir: welcome-{{version}}
depends:
  buildtime:
  - go
  runtime:
  - gtk
script: DESTDIR=${pkgupd_pkgdir} ./scripts/install.sh
