id: wpebackend-fdo
version: 1.10.0
about: The Freedesktop.org backend for WPE WebKit and the WPE renderer
sources:
- https://wpewebkit.org/releases/wpebackend-fdo-1.10.0.tar.xz
build-dir: wpebackend-fdo-1.10.0
depends:
  runtime:
  - libepoxy
  - libwpe
  - wayland
configure: --buildtype=release
