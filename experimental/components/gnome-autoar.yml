id: gnome-autoar
version: 0.4.0
about: Package provides a framework for automatic archive extraction, compression,
  and management
sources:
- https://download.gnome.org/sources/gnome-autoar/0.4/gnome-autoar-0.4.0.tar.xz
build-dir: gnome-autoar-0.4.0
depends:
  buildtime:
  - vala
  runtime:
  - libarchive
  - gtk
configure: --buildtype=release -D vapi=true -D tests=true
