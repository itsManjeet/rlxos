id: gnome-keyring
version: 40.0
about: A daemon that keeps passwords and other secrets for users
sources:
- https://download.gnome.org/sources/gnome-keyring/40/gnome-keyring-40.0.tar.xz
build-dir: gnome-keyring-40.0
depends:
  runtime:
  - dbus
  - gcr
  - pam
  - libxslt
  - openssh
pre-script: 'sed -i "s:\"/desktop:\"/org:" schema/*.xml

  '
