id: libnotify
version: 0.7.9
about: Library is used to send desktop notifications to a notification daemon
sources:
- https://download.gnome.org/sources/libnotify/0.7/libnotify-0.7.9.tar.xz
build-dir: libnotify-0.7.9
depends:
  buildtime:
  - gobject-introspection
  runtime:
  - gtk
configure: --buildtype=release -D gtk_doc=false -D man=false
