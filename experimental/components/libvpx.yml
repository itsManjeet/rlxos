id: libvpx
version: 1.10.0
about: Reference implementations of the VP8 Codec
sources:
- https://github.com/webmproject/libvpx/archive/v1.10.0/libvpx-1.10.0.tar.gz
build-dir: libvpx-1.10.0
depends:
  buildtime:
  - yasm
  - which
pre-script: 'sed -i "s/cp -p/cp/" build/make/Makefile

  '
configure: --prefix=/usr --enable-shared --disable-static
