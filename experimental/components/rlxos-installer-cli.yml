id: rlxos-installer-cli
version: 2307
release: 1
about: rlxos cli installer
depends:
  runtime:
    - bash

script: |
  install -Dm0755 -t ${pkgupd_pkgdir}/usr/bin/ ${FILES}/rlxos-installer-cli/rlxos-installer-cli.sh