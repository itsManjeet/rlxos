id: seatd
version: 0.6.3
about: A minimal seat management daemon and universal seat management library
sources:
- seatd-0.6.3.tar.gz::https://git.sr.ht/~kennylevinsen/seatd/archive/0.6.3.tar.gz
build-dir: seatd-0.6.3
depends:
  buildtime:
  - meson
  runtime:
  - systemd
configure: -Dexamples=disabled -Dserver=enabled -Dlibseat-logind=systemd -Dlogind=enabled
