id: alsa-lib
version: 1.2.12
about: ALSA library to access the ALSA sound interface

depends:
  - components/glibc.yml

sources:
  - https://www.alsa-project.org/files/pub/lib/alsa-lib-%{version}.tar.bz2
