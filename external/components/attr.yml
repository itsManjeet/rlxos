id: attr
version: 2.5.2
about: Utilities to administer the extended attributes on filesystem objects

build-dir: attr-%{version}
configure: >
  --sysconfdir=/etc
  --disable-static

depends:
  - components/glibc.yml

sources:
  - https://download.savannah.gnu.org/releases/attr/attr-%{version}.tar.gz
