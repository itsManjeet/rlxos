id: dbus-c++
version: 0.9.0
about: A C++ API for D-BUS

pre-script: |
  # patch -Np0 -i /assets/dbus-c++/gcc47.patch
  # patch -Np1 -i /assets/dbus-c++/disable-threading.patch
  # patch -Np1 -i /assets/dbus-c++/fix-writechar.patch
  # patch -Np1 -i /assets/dbus-c++/template-operators.patch
  autoreconf -vi

configure: >-
  --disable-static
  --disable-ecore
  --enable-glib

depends:
  - components/dbus.yml
  - components/libgcc.yml
  - components/glib.yml
sources:
  - https://sourceforge.net/projects/dbus-cplusplus/assets/dbus-c++/%{version}/libdbus-c++-%{version}.tar.gz

# TODO: fix patches