id: diffutils
version: 3.10
about: Programs that show the differences between files or directories
sources:
  - https://ftp.gnu.org/gnu/diffutils/diffutils-%{version}.tar.xz

depends:
  - components/bash.yml
