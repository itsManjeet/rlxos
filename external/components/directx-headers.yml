id: directx-headers
version: 1.614.0
about: DirectX headers for using D3D12

build-type: meson

configure: >-
  -D build-test=false

build-depends:
  - components/meson.yml

sources:
  - directx-headers-%{version}.tar.gz::https://github.com/microsoft/DirectX-Headers/archive/refs/tags/v%{version}.tar.gz
