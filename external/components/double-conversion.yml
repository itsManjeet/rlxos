id: double-conversion
version: 3.3.0
about: Binary-decimal and decimal-binary routines for IEEE doubles

configure: >-
  -DBUILD_SHARED_LIBS=ON
depends:
  - components/libgcc.yml

build-depends:
  - components/cmake.yml

sources:
- https://github.com/google/double-conversion/archive/v%{version}/double-conversion-%{version}.tar.gz