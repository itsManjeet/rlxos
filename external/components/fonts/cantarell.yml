id: cantarell
version: 0.303.1
about: Humanist sans serif font

build-depends:
  - components/meson.yml

sources:
  - https://download.gnome.org/sources/cantarell-fonts/%{version:2}/cantarell-fonts-%{version}.tar.xz

configure: >
  -D useprebuilt=true
