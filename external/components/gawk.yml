id: gawk
version: 5.3.0
about: GNU version of awk
pre-script: |
  sed -i 's/extras//' Makefile.in

sources:
  - https://ftp.gnu.org/gnu/gawk/gawk-%{version}.tar.xz

depends:
  - components/libmpfr.yml
