id: glibmm
version: 2.80.0
about: A set of C++ bindings for GLib

variables:
  run-autogen: false

depends:
  - components/glib.yml
  - components/libsigc++.yml
sources:
  - https://download.gnome.org/sources/glibmm/%{version:2}/glibmm-%{version}.tar.xz
