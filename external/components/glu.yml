id: glu
version: 9.0.3
about: The Mesa OpenGL Utility library

configure: >-
  -D gl_provider=gl
  --buildtype=release

depends:
  - components/mesa.yml
sources:
  - https://mesa.freedesktop.org/archive/glu/glu-%{version}.tar.xz
