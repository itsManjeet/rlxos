id: gnome-shell-extension-desktop-icons-ng
version: "46"
about:
  A fork from the official desktop icons project, with several enhancements like
  Drag'n'Drop.
release: 0
depends:
  - components/gnome/gnome-shell.yml
build-depends:
  - components/meson.yml
sources:
  - https://gitlab.com/rastersoft/desktop-icons-ng/-/archive/%{version}/desktop-icons-ng-%{version}.tar.gz
configure: --prefix=/usr --localedir=share/locale
