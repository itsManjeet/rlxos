id: initramfs
version: 0.3.0
about: avyos initramfs tool

script: |-
  install -v -D -m 644 /assets/init.in.sh  %{install-root}%{datadir}/initramfs/init.in
  install -v -D -m 755 /assets/mkinitramfs.sh  %{install-root}/usr/bin/mkinitramfs

depends:
  - components/bash.yml
  - components/kmod.yml
  - components/cpio.yml
  - components/zstd.yml
