id: libburn
version: 1.5.6
about: A library for writing preformatted data onto optical media CD, DVD and BD (Blu-Ray)
sources:
  - https://files.libburnia-project.org/releases/libburn-%{version}.tar.gz

configure: >-
  --disable-static

depends:
  - components/glibc.yml
