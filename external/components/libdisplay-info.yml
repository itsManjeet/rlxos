id: libdisplay-info
version: 0.1.1
about: EDID and DisplayID library

sources:
  - https://gitlab.freedesktop.org/emersion/libdisplay-info/-/releases/%{version}/downloads/%{id}-%{version}.tar.xz

depends:
  - components/hwdata.yml
