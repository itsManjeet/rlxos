id: libev
version: 4.33
about: A full featured and high performance event loop
sources:
  - http://dist.schmorp.de/libev/libev-%{version}.tar.gz

variables:
  run-autogen: false

depends:
  - components/glibc.yml
