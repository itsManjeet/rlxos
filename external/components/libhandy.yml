id: libhandy
version: 1.8.3
about: Provides additional GTK UI widgets for use in developing user interfaces

configure: >-
  --buildtype=release

depends:
  - components/gtk.yml
build-depends:
  - components/vala.yml
  - components/gobject-introspection.yml
  - components/meson.yml
sources:
  - https://download.gnome.org/sources/libhandy/%{version:2}/libhandy-%{version}.tar.xz
