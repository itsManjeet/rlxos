id: libisofs
version: 1.5.6
about: A library to create an ISO-9660 filesystem with extensions like RockRidge or Joliet
sources:
  - https://files.libburnia-project.org/releases/libisofs-%{version}.tar.gz

configure: >-
  --disable-static

depends:
  - components/acl.yml
  - components/zlib.yml
