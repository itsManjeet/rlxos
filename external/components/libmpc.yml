id: libmpc
version: 1.3.1
about: Library for the arithmetic of complex numbers with arbitrarily high precision and correct rounding of the result

sources:
  - https://ftp.gnu.org/gnu/mpc/mpc-%{version}.tar.gz

depends:
  - components/libmpfr.yml

configure: >-
  --disable-static
