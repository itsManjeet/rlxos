id: libndp
version: 1.9
about: Library provides a wrapper for IPv6 Neighbor Discovery Protocol
depends:
  - components/glibc.yml
sources:
  - libndp-%{version}.tar.gz::https://github.com/jpirko/libndp/archive/refs/tags/v%{version}.tar.gz

build-type: autotools