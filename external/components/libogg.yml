id: libogg
version: 1.3.5
about:
  Library or creating (encoding) or playing (decoding) a single physical bit
  stream

depends:
  - components/glibc.yml

sources:
  - https://downloads.xiph.org/releases/ogg/libogg-%{version}.tar.xz
