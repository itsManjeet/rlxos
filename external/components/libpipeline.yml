id: libpipeline
version: 1.5.7
about:
  Library for manipulating pipelines of subprocesses in a flexible and convenient
  way

depends:
  - components/glibc.yml
sources:
  - https://download.savannah.gnu.org/releases/libpipeline/libpipeline-%{version}.tar.gz
