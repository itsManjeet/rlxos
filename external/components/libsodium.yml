id: libsodium
version: 1.0.18
about: P(ortable|ackageable) NaCI-based crypto library

run-autogen: false

depends:
  - components/glibc.yml

build-depends:
  - components/libtool.yml
sources:
  - https://download.libsodium.org/libsodium/releases/libsodium-%{version}.tar.gz
