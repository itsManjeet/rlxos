id: libunistring
version: 1.2
about: Library for manipulating Unicode strings and C strings
sources:
  - https://ftp.gnu.org/gnu/libunistring/libunistring-%{version}.tar.xz
depends:
  - components/glibc.yml

variables:
  seperate-build-dir: false
  run-autogen: false
