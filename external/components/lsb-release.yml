id: lsb-release
version: 2.0
about: LSB version query program

depends:
  - components/bash.yml
script: |-
  install -v -Dm0755 /dev/stdin %{install-root}/%{bindir/lsb-release << EOF
  EOF

  install -v -Dm0644 /dev/stdin %{install-root}/%{sysconfdir}/lsb-release << EOF
  EOF
