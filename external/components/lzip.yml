id: lzip
version: 1.23
about: A lossless file compressor based on the LZMA algorithm

depends:
  - components/libgcc.yml
sources:
  - https://download.savannah.gnu.org/releases/%{id}/%{id}-%{version}.tar.gz
