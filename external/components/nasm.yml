id: nasm
version: 2.16.03
about: An 80x86 assembler designed for portability and modularity

variables:
  seperate-build-dir: false
  run-autogen: false

depends:
  - components/glibc.yml

sources:
  - https://www.nasm.us/pub/nasm/releasebuilds/%{version}/nasm-%{version}.tar.xz
