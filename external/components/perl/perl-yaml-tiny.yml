id: perl-yaml-tiny
version: 1.73
about: Read/Write YAML files with as little code as possible

depends:
  - components/perl.yml
sources:
  - https://cpan.metacpan.org/authors/id/E/ET/ETHER/YAML-Tiny-%{version}.tar.gz

environ:
  - PERL_MM_USE_DEFAULT=1

configure: INSTALLDIRS=vendor
