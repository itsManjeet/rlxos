id: popt
version: 1.19
about: Libraries which are used by some programs to parse command-line options
sources:
  - http://ftp.rpm.org/popt/releases/popt-1.x/popt-%{version}.tar.gz

depends:
  - components/glibc.yml

build-depends:
  - components/libtool.yml
