id: rdfind
version: 1.6.0
about: Redundant data find - a program that finds duplicate files.

depends:
  - components/libgcc.yml
  - components/glibc.yml
  - components/nettle.yml

sources:
  - https://rdfind.pauldreik.se/rdfind-%{version}.tar.gz
