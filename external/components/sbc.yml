id: sbc
version: 2.0
about: A digital audio encoder and decoder used to transfer data to Bluetooth audio output devices like headphones or loudspeakers

configure: >-
  --disable-static
  --disable-tester

depends:
  - components/libsndfile.yml
sources:
  - https://www.kernel.org/pub/linux/bluetooth/sbc-%{version}.tar.xz
