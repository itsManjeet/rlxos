id: spirv-headers
version: 1.4.321.0
about: SPIR-V Headers

sources:
    - https://github.com/KhronosGroup/SPIRV-Headers/archive/vulkan-sdk-%{version}/SPIRV-Headers-%{version}.tar.gz

build-depends:
    - components/glibc.yml
    - components/cmake.yml
