id: sysprof
version: 3.40.1
about: |
  A statistical and system-wide profiler for Linux

pre-script: |-
  mkdir -p /usr/local/include

depends:
  - components/json-glib.yml
  - components/libdazzle.yml
  - components/polkit.yml
sources:
  - https://download.gnome.org/sources/sysprof/%{version:2}/sysprof-%{version}.tar.xz
