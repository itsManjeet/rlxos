id: tar
version: 1.35
about: Package provides the ability to create tar archives as well as perform various other kinds of archive manipulation

depends:
  - components/acl.yml

environ:
  - FORCE_UNSAFE_CONFIGURE=1

sources:
  - https://ftp.gnu.org/gnu/tar/tar-%{version}.tar.xz
