id: tllist
version: 1.1.0
about: A typed linked list C header file only library

sources:
  - tllist-%{version}.tar.gz::https://codeberg.org/dnkl/tllist/archive/%{version}.tar.gz

depends:
  - components/glibc.yml

build-depends:
  - components/meson.yml
