id: vulkan-headers
version: 1.3.290
about: header files necessary to build and link applications against the Vulkan API

configure: >-
  -GNinja

build-depends:
  - components/cmake.yml

sources:
  - https://github.com/KhronosGroup/Vulkan-Headers/archive/v%{version}/Vulkan-Headers-%{version}.tar.gz
