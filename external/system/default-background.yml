id: default-background
version: 0.0.1
about: Default background

depends:
  - components/themes/fluent-wallpaper.yml

script: |-
  mkdir -p %{install-root}%{datadir}/backgrounds
  cd %{install-root}%{datadir}/backgrounds

  ln -sv Fluent-mountain-day.png default.png