options:
  arch:
    type: arch
    description: Target architecture
    variable: arch
    values:
      - x86_64

  variant:
    type: enum
    description: System Variant
    variable: variant
    values:
      - desktop
      - workstation
      - experimental

    default: experimental
