options:
  arch:
    type: arch
    description: Target architecture
    variable: arch
    values:
      - x86_64

  variant:
    type: enum
    description: Stability stream
    variable: variant
    values:
      - experimental
      - stable
    default: experimental