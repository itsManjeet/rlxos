options:
  arch:
    type: arch
    description: Target architecture
    variable: arch
    values:
      - x86_64

  variant:
    type: enum
    description: Target variant
    variable: variant
    values:
      - experimental
      - desktop
    default: experimental