variables:
  prefix: "/usr"
  sysconfdir: "/etc"
  libexecdir: "/usr/lib"
  libdir: "/usr/lib"
  bindir: "/usr/bin"
  sbindir: "/usr/bin"
  datadir: "/usr/share"
  localstatedir: "/var"
  triplet: "x86_64-linux-gnu"

  arch: "x86_64"

  skip-autogen: true
  no-build-dir: false