id: mousepad
version: 0.5.7
about: Simple text editor for xfve
depends:
  buildtime:
    - desktop-file-utils
    - gtksourceview4
    - hicolor-icon-theme
    - intltool
    - appimagetool
sources:
  - https://archive.xfce.org/src/apps/mousepad/0.5/mousepad-{{version}}.tar.bz2
type: app
category: accessories
bundle: true
tags:
  - editor
  - text-editor
  - file
post-script: |
  cd ${pkgupd_pkgdir}

  rm usr/share/{metainfo,polkit-1} -r
  cp usr/share/applications/org.xfce.mousepad.desktop {{id}}.desktop
  cp usr/share/icons/hicolor/scalable/apps/org.xfce.mousepad.svg {{id}}.png
  sed -i 's#org.xfce.mousepad#{{id}}#' {{id}}.desktop

  glib-compile-schemas usr/share/glib-2.0/schemas/

  install -v -m 0755 ${FILES_DIR}/AppRun AppRun