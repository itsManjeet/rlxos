 #!/bin/sh
HERE="$(dirname "$(readlink -f "${0}")")"
export LD_LIBRARY_PATH=${HERE}/usr/lib:${LD_LIBRARY_PATH}
export QT_PLUGIN_PATH=${HERE}/usr/lib/qt5/plugins:${QT_PLUGIN_PATH}
exec ${HERE}/usr/bin/qtcreator ${@}