id: gnome-font-viewer
version: 40.0
about: A font viewer utility for GNOME
depends:
  runtime:
    - gtk
    - gnome-desktop
    - libhandy
  buildtime:
    - meson
    - git
sources:
  - https://download.gnome.org/sources/{{id}}/40/{{id}}-{{version}}.tar.xz