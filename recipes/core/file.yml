id: file
version: 5.40
about: |
  Utility for determining the type of a given file or files.

depends:
  runtime:
    - glibc
    - zlib
    - bzip2

sources:
  - https://astron.com/pub/file/file-5.40.tar.gz

packages:
  - id: pkg
    dir: file-5.40
