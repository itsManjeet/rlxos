id: firmware
version: 20220708
about: |
  Firmware files for Linux

sources:
  - https://git.kernel.org/pub/scm/linux/kernel/git/firmware/linux-firmware.git/snapshot/linux-firmware-{{version}}.tar.gz

script: |
  patch -Np1 -i ${FILES_DIR}/WHENCE-Correct-dangling-symlink.patch
  make DESTDIR=${pkgupd_pkgdir} FIRMWARE=/usr/lib/firmware install