id: inspector
version: 0.1.3
commit: 68a578d5f04468ba012977ff40cb1d04cb9f7851
about: |
  Inspect on Application directory for changes

depends:
  runtime:
    - libnotify
    - libappimage
  
  buildtime:
    - cmake

sources:
  - inspector-{{version}}-{{commit}}.tar.gz::https://github.com/itsManjeet/inspector/archive/{{commit}}.tar.gz
build-dir: inspector-{{commit}}