id: installer
version: 2200
release: 3
commit: f7e89eb930cf87e090625421fb7d1c07fb230985
about: rlxos System Installer

depends:
  buildtime:
    - go

sources:
  - installer-{{commit}}.tar.gz::https://github.com/itsManjeet/installer/archive/{{commit}}.tar.gz

build-dir: installer-{{commit}}
script: |
  mkdir -p ${pkgupd_pkgdir}/usr/bin/
  go build -o ${pkgupd_pkgdir}/usr/bin/installer cmd/cli/cli.go