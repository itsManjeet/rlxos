id: jansson
version: 2.13.1
about: |
  Package contains a library used to encode, decode, and manipulate JSON data

sources:
  - https://digip.org/jansson/releases/jansson-2.13.1.tar.gz

packages:
  - id: pkg
    dir: jansson-2.13.1
 
    flags:
      - id: configurator
        value: configure
      - id: configure
        value: --disable-static
