id: jetring
version: "0.30"
about: GPG keyring maintenance using changesets
depends:
  runtime:
    - gnupg
    - perl
sources:
  - https://mirrors.kernel.org/debian/pool/main/j/jetring/jetring_{{version}}.tar.xz
script: |
  make DESTDIR=${pkgupd_pkgdir} install