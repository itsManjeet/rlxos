id: libburn
version: 1.5.4
about: |
  A library for writing preformatted data onto optical media: CD, DVD and BD (Blu-Ray)

sources:
  - https://files.libburnia-project.org/releases/libburn-1.5.4.tar.gz

packages:
  - id: lib
    dir: libburn-1.5.4
    flags:
      - id: configure
        value: --disable-static