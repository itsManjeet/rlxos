id: libjit
version: 0.1.4
about: Generic jit compiler library
depends:
  buildtime:
    - gawk
    - bison
    - flex
sources:
  - http://git.savannah.gnu.org/cgit/libjit.git/snapshot/libjit-{{version}}.tar.gz
build-dir: libjit-{{version}}
pre-script: |
  ./bootstrap
