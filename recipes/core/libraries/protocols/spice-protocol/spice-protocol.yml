id: spice-protocol
version: 0.14.4
about: Headers for SPICE protocol
depends:
  buildtime:
    - meson
sources:
  - https://gitlab.freedesktop.org/spice/spice-protocol/-/archive/v{{version}}/spice-protocol-v{{version}}.tar.gz

post-script: |
  install -v -D -m 0644 COPYING ${pkgupd_pkgdir}/usr/share/doc/{{id}}/LICENSE