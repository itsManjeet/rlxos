id: libsass
version: 3.6.5
about: |
  A CSS pre-processor language

sources:
  - https://github.com/sass/libsass/archive/{{version}}/libsass-{{version}}.tar.gz

build-dir: libsass-{{version}}
script: |
  autoreconf -fi
  ./configure --prefix=/usr
  make
  make DESTDIR=${pkgupd_pkgdir} install
