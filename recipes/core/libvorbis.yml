id: libvorbis
version: 1.3.7
about: |
  Library for general purpose audio and music encoding format

depends:
  runtime:
    - libogg

sources:
  - https://downloads.xiph.org/releases/vorbis/libvorbis-1.3.7.tar.xz

packages:
  - id: lib
    dir: libvorbis-1.3.7

    flags:
      - id: configurator
        value: configure
      - id: configure
        value: --disable-static
    