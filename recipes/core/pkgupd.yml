id: pkgupd
version: 2201
release: 1
about: |
  An Extensible package manager for rlxos

depends: 
  runtime: 
    - libyaml-cpp
    - curl
    - zstd
    - libarchive
    - tar
    - bash
    
  buildtime: 
    - cmake

sources:
  - pkgupd-{{version}}.tar.gz::https://github.com/rlxos/pkgupd/archive/refs/heads/{{version}}.tar.gz

packages:
  - id: pkg
    dir: pkgupd-{{version}}