id: lsb-release
version: 2.0
about: LSB version query program
depends:
  runtime:
    - bash
license: GPL
script: |
  install -v -D -m 0755 ${FILES_DIR}/lsb-release.sh ${pkgupd_pkgdir}/usr/bin/lsb-release
  install -v -D -m 0644 ${FILES_DIR}/lsb-release -t ${pkgupd_pkgdir}/etc