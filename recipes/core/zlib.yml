id: zlib
version: 1.2.11
about: |
  Compression and decompression routines

sources:
  - https://apps.rlxos.dev/sources/zlib-{{version}}.tar.xz

build-dir: zlib-{{version}}
build-type: autoconf

configure: >
  --prefix=/usr 