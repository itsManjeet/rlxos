id: zlib
version: 1.2.12
about: |
  Compression and decompression routines

sources:
  - https://zlib.net/zlib-{{version}}.tar.xz

build-dir: zlib-{{version}}
build-tool: autoconf

environ:
  - CFLAGS+=' -ffat-lto-objectss'
configure: >
  --prefix=/usr 