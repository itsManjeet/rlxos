id: zlib
version: 1.2.11
about: |
  Compression and decompression routines

sources:
  - zlib-{{version}}.tar.gz::https://github.com/madler/zlib/archive/refs/tags/v{{version}}.tar.gz

build-dir: zlib-{{version}}
build-type: autoconf

configure: >
  --prefix=/usr 
