id: enet
version: 1.3.17
about: A relatively thin, simple and robus network communication layer on top of UDP
depends:
  runtime:
    - glibc
license: custom
sources:
  - http://enet.bespin.org/download/{{id}}-{{version}}.tar.gz

post-script: |
  install -v -D -m 0644 LICENSE -t ${pkgupd_pkgdir}/usr/share/doc/{{id}}/