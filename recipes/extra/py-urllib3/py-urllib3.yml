id: py-urllib3
version: 1.26.9
about: HTTP library with thread-safe connection pooling and file post support
depends:
  buildtime:
    - py-setuptools
    - py-socks
sources:
  - https://github.com/urllib3/urllib3/archive/{{version}}/{{id}}-{{version}}.tar.gz
