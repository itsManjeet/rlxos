id: raft
version: 0.13.0
about: C implementation of the raft consensus protocol
depends:
  runtime:
    - libuv
sources:
  - raft-{{version}}.tar.gz::https://github.com/canonical/raft/archive/v{{version}}.tar.gz
build-dir: raft-{{version}}
pre-script: |
  autoreconf -i