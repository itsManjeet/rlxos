id: groff
version: 1.22.4
about: |
  Programs for processing and formatting text

depends:
  runtime:
    - gcc
    - perl
    - texinfo

sources:
  - https://ftp.gnu.org/gnu/groff/groff-1.22.4.tar.gz

environ:
  - PAGE=A4
  
packages:
  - id: pkg
    dir: groff-1.22.4

    flags:
      - id: compile
        value: -j1
        only: true