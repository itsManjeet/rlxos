id: gzip
version: 1.10
about: |
  Programs for compressing and decompressing files

depends:
  runtime:
    - bash
    - less

sources:
  - https://ftp.gnu.org/gnu/gzip/gzip-1.10.tar.xz

packages:
  - id: pkg
    dir: gzip-1.10