id: libgxps
version: 0.3.2
about: |
  Provides an interface to manipulate XPS documents

depends:
  runtime:
    - gtk
    - lcms
    - libarchive
    - libjpeg-turbo
    - libtiff
    - libxslt

sources:
  - https://download.gnome.org/sources/libgxps/0.3/libgxps-0.3.2.tar.xz

packages:
  - id: pkg
    dir: libgxps-0.3.2
    flags:
      - id: configure
        value: --buildtype=release