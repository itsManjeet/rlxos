id: libmd
version: 1.0.3
about: |
  Message Digest functions from BSD systems

depends:
  runtime:
    - glibc

sources:
  - https://libbsd.freedesktop.org/releases/libmd-1.0.3.tar.xz

packages:
  - id: lib
    dir: libmd-1.0.3