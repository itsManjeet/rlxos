id: libportal
version: 0.4
about: |
  A library that contains GIO-style async APIs for most Flatpak portals

depends:
  runtime:
    - glib

sources:
  - https://github.com/flatpak/libportal/releases/download/0.4/libportal-0.4.tar.xz

packages:
  - id: pkg
    dir: libportal-0.4
    flags:
      - id: configure
        value: --buildtype=release -D gtk_doc=false