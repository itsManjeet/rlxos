id: libtiff
version: 4.3.0
about: |
  TIFF libraries and associated utilities

depends:
  buildtime:
    - cmake

sources:
  - https://download.osgeo.org/libtiff/tiff-4.3.0.tar.gz

packages:
  - id: pkg
    dir: tiff-4.3.0