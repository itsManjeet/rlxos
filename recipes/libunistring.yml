id: libunistring
version: 0.9.10
about: |
  Library for manipulating Unicode strings and C strings

depends:
  runtime:
    - glibc

sources:
  - https://ftp.gnu.org/gnu/libunistring/libunistring-0.9.10.tar.xz

packages:
  - id: pkg
    dir: libunistring-0.9.10

    flags:
      - id: configurator
        value: configure
        
      - id: configure
        value: --disable-static