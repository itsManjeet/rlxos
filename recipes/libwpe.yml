id: libwpe
version: 1.10.1
about: |
  A general purpose library for WPE WebKit and the WPE Renderer

depends:
  runtime:
    - libxkbcommon
    - mesa

sources:
  - https://wpewebkit.org/releases/libwpe-1.10.1.tar.xz

packages:
  - id: pkg
    dir: libwpe-1.10.1