id: libxft
version: 2.3.4
about: |
  FreeType based font drawing library for X

depends:
  runtime:
    - fontconfig
    - libxrender
  buildtime:
    - pkg-config

sources:
  - https://www.x.org/pub/individual/lib/libXft-2.3.3.tar.bz2

packages:
  - id: pkg
    dir: libXft-2.3.3