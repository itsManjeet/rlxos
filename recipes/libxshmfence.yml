id: libxshmfence
version: 1.3
about: |
  Library that exposes a event API on top of linux futexes

depends:
  runtime:
    - glibc
  buildtime:
    - util-macros
    - xorgproto

sources:
  - https://www.x.org/pub/individual/lib/libxshmfence-1.3.tar.bz2

packages:
  - id: pkg
    dir: libxshmfence-1.3