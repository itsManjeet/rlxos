id: parted
version: 3.4
about: |
  A disk partitioning and partition resizing tool

depends:
  runtime:
    - lvm2

sources:
  - https://ftp.gnu.org/gnu/parted/parted-3.4.tar.xz

packages:
  - id: pkg
    dir: parted-3.4