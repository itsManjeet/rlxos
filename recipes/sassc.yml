id: sassc
version: 3.6.2
about: |
  A CSS pre-processor language

sources:
  - https://github.com/sass/sassc/archive/3.6.2/sassc-3.6.2.tar.gz

depends:
  runtime:
    - libsass
build-dir: sassc-3.6.2
script: |
  autoreconf -fi
  ./configure --prefix=/usr --disable-static
  make
  make DESTDIR=${pkgupd_pkgdir} install
