id: wpebackend-fdo
version: 1.10.0
about: |
  The Freedesktop.org backend for WPE WebKit and the WPE renderer

depends:
  runtime:
    - libepoxy
    - libwpe
    - wayland

sources:
  - https://wpewebkit.org/releases/wpebackend-fdo-1.10.0.tar.xz

packages:
  - id: pkg
    dir: wpebackend-fdo-1.10.0
    flags:
      - id: configure
        value: >
          --buildtype=release