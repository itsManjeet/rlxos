id: xauth
version: 1.1
about: X.Org authorization settings program
depends:
  runtime:
    - libxmu
  buildtime:
    - util-macros
sources:
  - https://www.x.org/pub/individual/app/xauth-1.1.tar.bz2
packages:
  - id: pkg
    dir: xauth-1.1
    flags:
      - id: configurator
        value: configure