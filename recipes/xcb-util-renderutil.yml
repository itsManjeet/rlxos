id: xcb-util-renderutil
version: 0.3.9
about: |
  Additional extensions to the XCB library

depends:
  runtime:
    - libxcb

sources:
  - https://xcb.freedesktop.org/dist/xcb-util-renderutil-0.3.9.tar.bz2

packages:
  - id: pkg
    dir: xcb-util-renderutil-0.3.9
    flags:
      - id: configurator
        value: configure