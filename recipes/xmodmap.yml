id: xmodmap
version: 1.0.10
about: utility for modifying keymaps and button mappings
depends:
  runtime:
    - libx11
  buildtime:
    - util-macros
sources:
  - https://www.x.org/pub/individual/app/xmodmap-1.0.10.tar.bz2
packages:
  - id: pkg
    dir: xmodmap-1.0.10
    flags:
      - id: configurator
        value: configure