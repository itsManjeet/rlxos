id: zlib
version: 1.2.11
about: |
  Compression and decompression routines

sources:
  - https://zlib.net/zlib-1.2.11.tar.xz

packages:
  - id: pkg
    dir: zlib-1.2.11

    flags:
      - id: configurator
        value: configure

      - id: configure
        force: true
        value: >
          --prefix=/usr
